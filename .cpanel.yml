---
deployment:
  tasks:
    - export DEPLOYPATH=/home/boxgra6/titus-duc.calisearch.org/app
    - /bin/echo "Starting deployment to $DEPLOYPATH"
    - /bin/cp -R * $DEPLOYPATH
    - /bin/echo "Deployment completed successfully."
